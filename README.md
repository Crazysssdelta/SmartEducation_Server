# SmartEducation_Server
software system development
In the database:
all primary key of tables are called "id"
only "classes" and "students" the primary key "id" isn't auto-incremented
ohters are auto increaded
